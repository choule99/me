---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<div class="flex justify-center mb-8">
  <span class="inline-block px-6 py-2.5 text-sm font-semibold tracking-wider uppercase text-indigo-600 bg-indigo-100 rounded-full">
    {label}
  </span>
</div>
